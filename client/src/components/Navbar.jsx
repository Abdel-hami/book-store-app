import React, { useState } from 'react'

const Navbar = () => {
    const [isMenuopen, setMenuOpen] = useState(fasle);
    const [isSticky, setSticky] = useState(false);
    
  return (
    <div>Navbar</div>
  )
}

export default Navbar